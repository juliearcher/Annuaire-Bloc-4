<UserControl x:Class="Annuaire_Bloc_4.Views.SitesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Annuaire_Bloc_4.Views" xmlns:viewsmodels="clr-namespace:Annuaire_Bloc_4.ViewModels"
			 xmlns:controls="clr-namespace:Annuaire_Bloc_4.Controls"
			 d:DataContext="{d:DesignInstance Type=viewsmodels:SitesViewModel}"
			 mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<TextBlock Grid.Row="0" Text="Liste des sites" 
                   FontWeight="Bold" FontSize="18"
                   HorizontalAlignment="Center" VerticalAlignment="Center" />

		<controls:SitesList Grid.Row="1" DataContext="{Binding SiteListViewModel}"/>
	</Grid>
</UserControl>
