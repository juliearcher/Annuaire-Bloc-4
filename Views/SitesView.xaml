<UserControl x:Class="Annuaire_Bloc_4.Views.SitesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Annuaire_Bloc_4.Views" xmlns:viewsmodels="clr-namespace:Annuaire_Bloc_4.ViewsModels"
			 d:DataContext="{d:DesignInstance Type=viewsmodels:SitesViewModel}"
			 mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<TextBlock Grid.Row="0" Text="This is the Sites List Page" 
                   FontWeight="Bold" FontSize="18"
                   HorizontalAlignment="Center" VerticalAlignment="Center" />

		<ListView Grid.Row="1" Margin="0 10 0 0" ItemsSource="{Binding SiteList}">
			<ListView.View>
				<GridView>
					<GridViewColumn Header="Ville" DisplayMemberBinding="{Binding City}"/>
				</GridView>
			</ListView.View>
		</ListView>
	</Grid>
</UserControl>
